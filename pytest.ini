[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
addopts = -ra
markers =
    integration: tests requiring Home Assistant integration test runtime
    security: security-focused tests
