{
  "config": {
    "abort": {
      "single_instance_allowed": "Only a single instance is allowed."
    },
    "step": {
      "user": {
        "title": "Set up HA Easy Control",
        "description": "Create the HA Easy Control integration."
      }
    }
  },
  "options": {
    "error": {
      "invalid_cidr": "CIDR list must contain valid RFC1918 networks only.",
      "invalid_security_limits": "Security limits must be positive (clock skew may be zero)."
    },
    "step": {
      "init": {
        "title": "HA Easy Control network policy",
        "data": {
          "local_only": "Restrict guest API to local network only",
          "require_device_binding": "Require device key registration during pairing",
          "require_action_proof": "Require signed proof for action execution",
          "default_require_admin_approval": "Require admin approval by default for new pairing passes",
          "allowed_cidrs": "Allowed CIDR ranges (RFC1918 only, comma separated)",
          "pair_rate_limit_per_min": "Pair endpoint requests per minute",
          "action_rate_limit_per_min": "Action/nonce requests per minute",
          "qr_rate_limit_per_min": "QR image requests per minute",
          "states_rate_limit_per_min": "Entity states requests per minute",
          "nonce_ttl_seconds": "Action nonce TTL (seconds)",
          "action_proof_clock_skew_seconds": "Allowed action proof clock skew (seconds)"
        }
      }
    }
  }
}
